version: 2

models:
  - name: airbnb_neighbourhood
    description: "Airbnb Rio de Janeiro neighbourhoods"
    columns:
      - name: neighbourhood_name
        description: neighbourhood name
        tests:
          - not_null
          - unique
      - name: neighbourhood_id
        description: neighbourhood id
        tests:
          - not_null
          - unique